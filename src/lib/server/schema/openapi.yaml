components:
  schemas:
    MessageResponse:
      properties:
        message:
          title: Message
          type: string
      required:
      - message
      title: MessageResponse
      type: object
info:
  title: FastAPI
  version: 0.1.0
openapi: 3.1.0
paths:
  /api/message/:
    get:
      operationId: get_message_api_message__get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
          description: Successful Response
      summary: Get Message
